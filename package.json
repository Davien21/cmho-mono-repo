{
  "name": "cmho-monorepo",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "typecheck": "tsc -b",
    "lint": "turbo run lint",
    "dev": "turbo run dev",
    "build": "turbo run build"
  },
  "devDependencies": {
    "turbo": "^2.5.3"
  },
  "packageManager": "pnpm@10.8.0"
}
