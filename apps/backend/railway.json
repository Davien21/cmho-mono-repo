{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "pnpm install --frozen-lockfile",
    "useMetalBuildEnvironment": true
  },
  "deploy": {
    "startCommand": "pnpm --filter @cmho/backend start"
  }
}
